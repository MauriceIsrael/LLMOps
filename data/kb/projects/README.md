# Project instances

One directory per engagement. Copy `_template/` to start.

Instances **reference** core assets by identifier and record only their deltas.
An instance that restates the core has already begun to diverge from it.

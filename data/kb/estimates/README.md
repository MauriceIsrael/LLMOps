# Estimates

Effort models with their variance drivers. The drivers matter more than the
numbers: a range without the conditions that move it is a false promise.

Every harvest appends to `actuals`. A model with an empty `actuals` list is an
educated guess and its `confidence` must stay `assumed`.

# Risks

Reusable risk registers. `materialized_in` is the field that gives this file its
value over time: it records which engagements actually hit each risk, which is
what lets a bid team argue likelihood from evidence rather than from instinct.
